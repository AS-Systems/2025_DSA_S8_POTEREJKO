<UserControl x:Class="LibraryManager.View.CustomControls.Buttons.ComparisonOperationButtonControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManager.View.CustomControls.Buttons"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation
            Storyboard.TargetName="ToggleButton"
            Storyboard.TargetProperty="Opacity"
            From="1" To="0" Duration="0:0:0.1" />
        </Storyboard>

        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation
            Storyboard.TargetName="ToggleButton"
            Storyboard.TargetProperty="Opacity"
            From="0" To="1" Duration="0:0:0.1" />
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Button x:Name="ToggleButton" Content=">" Click="ToggleButton_Click" Width="50" Height="50" 
        FontSize="30" Background="#faffc5" Foreground="#3a3960" BorderBrush="#5e686d"/>
    </Grid>
</UserControl>
