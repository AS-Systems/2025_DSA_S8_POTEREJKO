<UserControl x:Class="LibraryManager.View.CustomControls.Capsules.Capsule"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryManager.View.CustomControls.Capsules"
             mc:Ignorable="d" 
             Name="CapsuleControl"
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
          Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <Border CornerRadius="{Binding CapsuleRadius, ElementName=CapsuleControl}" Background="{Binding CapsuleBackground, ElementName=CapsuleControl}">
            <Grid>
                <!-- Your content here -->
            </Grid>
        </Border>
    </Grid>
</UserControl>
